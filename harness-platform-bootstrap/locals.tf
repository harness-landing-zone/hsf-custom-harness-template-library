# Bootstrap entrypoint locals.
# Org-level locals (tags, identifiers, config merging) are handled inside the
# harness-organization module. Add account-level locals here only if account-scoped
# resources are introduced in this entrypoint in future.
