---
########################
# Harness User Group — Project 1 Release Managers  (Account Level)
#
# This is the ACCOUNT-LEVEL definition of the group.
# Project-level role bindings live in:
#   platform-configs/organizations/Test Organisasion/
#     projects/Project 1/groups/Project_1_Release_Managers.yaml
#
# Members of this group:
#   - Can approve Harness Approval Steps on Project 1 pipelines
#     (configure the pipeline's Approval Step to reference this group)
#   - Receive the Release_Manager role at project scope via the
#     platform-configs cross-scope binding below
#   - Can trigger the full pipeline including the Production stage
#   - Can rollback Production environments
#   - Can manage and override deployment freeze windows
#
# SSO Configuration (uncomment to enable):
# linked_sso_type: "SAML"
# linked_sso_id: <your-sso-provider-id>
# sso_group_id: <idp-release-managers-group-id>
########################
description: "Release Managers for Application One  approve and deploy to production"

tags:
  purpose: ReleaseManagers
  project: Organisasion Test Project1
  managed_by: solutions-factory

role_bindings:
  # Account-level shared resource visibility (connectors, secrets, templates)
  - role: Shared_Resource_Access
    resource_group: _all_account_level_resources
