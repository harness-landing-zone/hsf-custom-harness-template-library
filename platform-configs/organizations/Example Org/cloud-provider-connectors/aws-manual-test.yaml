type: aws
name: "AWS Manual Connector Test"
description: "Test connector using manual key auth — should be silently skipped by org entrypoint"
execute_on_delegate: true
force_delete: false
tags:
  owner: platform-team
  purpose: test

manual_authentication:
  access_key_ref: "account.aws_access_key"
  secret_key_ref: "account.aws_secret_key"
  delegate_selectors:
    - "aws-delegate"
  region: "eu-west-2"
